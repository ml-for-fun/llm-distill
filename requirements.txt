# Core dependencies for LLM distillation
# Install with: pip install -r requirements.txt

# HuggingFace ecosystem
huggingface-hub>=0.20.0
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0

# Deep learning framework
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Tokenizers and model formats
tokenizers>=0.15.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# NumPy compatibility (must be <2.0 for current PyTorch)
numpy>=1.24.0,<2.0

# Efficient training
bitsandbytes>=0.41.0  # 8-bit training
peft>=0.7.0  # LoRA and parameter-efficient fine-tuning

# Progress and utilities
tqdm>=4.66.0
scikit-learn>=1.3.0  # for metrics and evaluation
matplotlib>=3.7.0  # for visualization

# Jupyter support (optional but useful)
jupyter>=1.0.0
ipywidgets>=8.1.0

# Configuration and logging
pyyaml>=6.0
tensorboard>=2.15.0
wandb>=0.16.0  # Weights & Biases for experiment tracking (optional)
